Captured from `https://api1.blocktank.to/api/fx/rates/btc` at
`2026-09-13T16:20:49Z`. The JSON body is pinned verbatim in the adjacent
fixture file; it contains the production `{ "tickers": [...] }` envelope and
the unique `BTCUSD` ticker used by the parser.
